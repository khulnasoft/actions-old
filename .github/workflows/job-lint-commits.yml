name: lint-commits

on: workflow_call

jobs:
  lint-commits:
    timeout-minutes: 5

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.1.7

      - uses: ghcr.io/khulnasoft-lab/action-commit-lint@2.1.17
        with:
          config: .github/linters/.commit-lint.yml
